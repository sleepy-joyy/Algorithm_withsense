function solution(n) {
    
    let jump = 0;

    while(n > 0) { 
        if(n % 2 === 0) n/=2
        else { 
            n--
            jump++ 
        }
    }
    return jump;   
}


/* k칸 점프  --> k만큼 배터리닳
 순간이동 : 현재 온 거리 2배 순간이동 : 배터리 안닳
 순간이동 횟수는 상관이 없다

도착지점에서 절반씩 내려와보자..?
5-> 2.5? 이건안되잖슴.. -> 1칸점프해서 4 -> 2 -> 1 -> jump -> 0 
홀수면 점프해야하고/ 짝수면 텔레포트가능

6-> 3 -> jump -> 2 -> 1 -> jump -> 0 */
